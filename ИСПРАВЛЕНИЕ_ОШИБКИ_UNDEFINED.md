# 🔧 ИСПРАВЛЕНИЕ: Ошибка "Cannot read properties of undefined"

## ❌ Проблема:

В логах была ошибка:
```
[ERROR] ❌ ERROR in sendEmailForNewLead: TypeError: Cannot read properties of undefined (reading 'name')
[INFO] Data: undefined
```

Функция `sendEmailForNewLead` вызывалась без данных или с `undefined`.

## ✅ Исправление:

1. **Добавлена проверка валидности данных** в начале функции `sendEmailForNewLead`
2. **Улучшена передача данных** из `doPost` и `onSheetEdit`
3. **Добавлены логи** для отладки

## 📋 Что нужно сделать:

### 1. Скопировать исправленный код

1. Откройте файл **GOOGLE_APPS_SCRIPT_CODE.js**
2. Скопируйте ВЕСЬ код (Ctrl+A, Ctrl+C)
3. Откройте https://script.google.com
4. Найдите ваш проект
5. УДАЛИТЕ весь старый код
6. ВСТАВЬТЕ новый код
7. Сохраните (Ctrl+S)

### 2. Проверить триггер

1. Убедитесь, что триггер `onSheetEdit` настроен (⏰ Triggers → должен быть в списке)
2. Если его нет - настройте его (см. файл НАСТРОЙКА_ТРИГГЕРА.md)

### 3. Протестировать

1. Отправьте тестовый лид через форму
2. Проверьте логи в Google Apps Script (View → Logs)
3. Должны быть записи:
   - `Prepared email data: {...}` - данные подготовлены правильно
   - `✅ Email sent successfully` - email отправлен

## 🔍 Что было исправлено:

### В функции `sendEmailForNewLead`:
```javascript
// Добавлена проверка в начале функции
if (!data || typeof data !== 'object') {
  throw new Error('Data parameter is required and must be an object');
}
```

### В функции `doPost`:
```javascript
// Данные теперь подготавливаются заранее
const emailData = {
  timestamp: new Date(),
  name: data.name || '',
  phone: data.phone || '',
  // ... остальные поля
};

// Затем передаются в функцию
emailSent = sendEmailForNewLead(emailData);
```

### В функции `onSheetEdit`:
```javascript
// Данные также подготавливаются заранее
const emailData = {
  timestamp: rowData[0] || new Date(),
  name: rowData[1] || '',
  phone: rowData[2] || '',
  // ... остальные поля
};

// Затем передаются в функцию
emailSent = sendEmailForNewLead(emailData);
```

## ✅ Результат:

После исправления:
- ✅ Функция `sendEmailForNewLead` всегда получает валидные данные
- ✅ Ошибки обрабатываются корректно с понятными сообщениями
- ✅ Email отправляется автоматически при каждом новом лиде



