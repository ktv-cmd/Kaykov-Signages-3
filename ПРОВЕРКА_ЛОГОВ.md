# 🔍 ПРОВЕРКА ЛОГОВ: Что происходит с триггером?

## ✅ Триггер работает!
Видно, что `onSheetEdit` запускается (5 раз в 5:17-5:18). Теперь нужно проверить **логи выполнения**, чтобы понять:
- ✅ Отправляются ли email?
- ❌ Есть ли ошибки?
- 📋 Что именно происходит при каждом запуске?

## 📋 Как проверить логи:

### ШАГ 1: Откройте логи выполнения

1. В Google Apps Script нажмите на **один из запусков триггера** (например, `Nov 4, 2025, 5:18:30 PM`)
2. Откроется страница с деталями выполнения
3. Прокрутите вниз до раздела **"Execution log"** или **"Логи выполнения"**

### ШАГ 2: Что искать в логах:

**✅ Хорошие признаки:**
```
[START] === onSheetEdit triggered ===
[INFO] Edit detected: Row X, Column Y...
[INFO] ✅ Valid lead data found: Name=..., Phone=...
[SUCCESS] ✅ Email sent successfully for row X
```

**❌ Проблемы:**
```
[ERROR] ❌ FATAL ERROR in onSheetEdit: ...
[WARNING] Row X doesn't have required fields...
[INFO] Edit is not a new lead row, skipping
[INFO] Wrong spreadsheet, skipping
```

### ШАГ 3: Скопируйте логи

1. Скопируйте **все записи** из Execution log
2. Отправьте мне, чтобы я мог увидеть, что происходит

## 🔍 Возможные причины, почему email не отправляется:

### 1. Триггер срабатывает не на те строки
**Признак:** В логах видно `Edit is not a new lead row, skipping`
**Решение:** Триггер правильно работает, но срабатывает на редактирование старых строк

### 2. Нет данных name/phone
**Признак:** `Row X doesn't have required fields: Name="", Phone=""`
**Решение:** Проверьте, что в новых строках заполнены колонки B (Name) и C (Phone)

### 3. Неправильный Sheet ID
**Признак:** `Wrong spreadsheet (XXX), skipping`
**Решение:** Убедитесь, что триггер работает с правильной таблицей

### 4. Ошибка отправки email
**Признак:** `❌ ERROR in sendEmailForNewLead: ...`
**Решение:** Нужно запустить `testEmail()` для разрешений

## 🧪 Тест: Добавьте новую строку вручную

1. Откройте Google Sheet
2. Добавьте **новую строку** в конец таблицы:
   - **Колонка A:** `=NOW()` (или текущая дата/время)
   - **Колонка B:** `Тест Триггера`
   - **Колонка C:** `+1(718) 478-4200`
   - **Колонка D:** `test@example.com` (опционально)
3. Нажмите **Enter**
4. Подождите 1-2 секунды
5. Проверьте логи выполнения для этого нового запуска триггера

## 📧 Проверка email:

После добавления новой строки:
1. Проверьте почту **ktv@kaykovmedia.com**
2. Проверьте папку **Spam** (иногда email попадает туда)
3. Подождите до 1 минуты - email может прийти с задержкой

## 🎯 Что мне нужно от вас:

**Отправьте:**
1. Логи выполнения из **одного из последних запусков** триггера (например, `5:18:30 PM`)
2. Подтвердите:
   - Проверяли ли вы почту **ktv@kaykovmedia.com**?
   - Проверяли ли папку **Spam**?
   - Добавляли ли вы новую строку вручную для теста?

С этой информацией я смогу точно определить, почему email не отправляется!



