# Быстрое решение проблемы с email

## ⚠️ Основная проблема: Email не приходит

Если email всё ещё не приходит, выполните следующие шаги ПО ПОРЯДКУ:

## Шаг 1: Обновите код в Google Apps Script

1. Откройте https://script.google.com
2. Найдите ваш проект
3. **Полностью замените** весь код на код из файла `GOOGLE_APPS_SCRIPT_CODE.js`
4. Сохраните (Ctrl+S)

## Шаг 2: Предоставьте разрешения (КРИТИЧНО!)

1. В редакторе Apps Script выберите функцию `testEmail` из выпадающего списка
2. Нажмите ▶️ **Run**
3. Google покажет диалог "Authorization required"
4. Нажмите **Review Permissions**
5. Выберите ваш Google аккаунт
6. Нажмите **Advanced** 
7. Нажмите **Go to [Your Project Name] (unsafe)**
8. Нажмите **Allow**
9. **ВАЖНО:** Подождите, пока тест завершится
10. Проверьте почту `ktv@kaykovmedia.com` - должно прийти тестовое письмо

## Шаг 3: Проверьте логи выполнения

1. В Apps Script откройте **Executions** (иконка часов ⏰ слева)
2. Найдите последнее выполнение `testEmail`
3. Нажмите на него
4. Посмотрите логи:
   - Если видите `✅ Test email sent successfully!` - email отправляется
   - Если видите `❌ ERROR` - проблема с отправкой

## Шаг 4: Переразверните Web App

1. Нажмите **Deploy** → **Manage deployments**
2. Если есть существующий deployment, нажмите ✏️ **Edit**
3. Нажмите **Deploy**
4. Убедитесь, что:
   - **Execute as:** Me
   - **Who has access:** Anyone

## Шаг 5: Протестируйте форму на сайте

1. Откройте ваш сайт
2. Заполните форму
3. Откройте консоль браузера (F12 или Cmd+Option+I)
4. Откройте вкладку **Console**
5. Отправьте форму
6. Вы должны увидеть:
   - `📤 Submitting to Google Sheets:` - данные отправляются
   - `✅ Request sent` - запрос отправлен

## Шаг 6: Проверьте логи doPost

1. В Apps Script откройте **Executions**
2. Найдите последнее выполнение `doPost`
3. Нажмите на него
4. Ищите в логах:
   - `=== doPost called ===` - функция вызвана
   - `✅ Email sent successfully` - email отправлен
   - `❌ ERROR sending email` - ошибка при отправке

## Шаг 7: Проверьте папку "Спам"

- Откройте почтовый ящик `ktv@kaykovmedia.com`
- Проверьте папку **Спам/Spam**
- Иногда письма попадают туда

## Шаг 8: Используйте тестовую страницу

1. Откройте файл `TEST_SCRIPT.html` в браузере
2. Нажмите кнопку **Test doGet** - должна быть успешная проверка
3. Нажмите кнопку **Send Test Data** - должны увидеть ответ
4. Проверьте, пришло ли письмо

## Возможные причины, если ничего не работает:

### 1. Не предоставлены разрешения на email
**Решение:** Выполните Шаг 2 заново

### 2. Неправильный email адрес
**Проверьте:** В `GOOGLE_APPS_SCRIPT_CODE.js` на строке 19 должно быть:
```javascript
const NOTIFICATION_EMAIL = 'ktv@kaykovmedia.com';
```

### 3. Google Workspace ограничения
**Если используется Google Workspace:**
- Администратор может блокировать отправку email
- Обратитесь к администратору для разрешения

### 4. Лимит отправки email
**Google Apps Script имеет лимит:**
- 100 email в день для бесплатного аккаунта
- Проверьте, не превышен ли лимит

### 5. Скрипт не получает данные
**Проверьте:**
- URL в `src/lib/googleSheets.ts` совпадает с URL из Apps Script
- В логах `Executions` есть записи о вызове `doPost`

## Альтернативное решение: Используйте Gmail API

Если `MailApp` не работает, код автоматически попробует `GmailApp`. Но для этого нужно:
1. Включить Gmail API в Apps Script
2. Предоставить дополнительные разрешения

## Проверка результатов:

После выполнения всех шагов проверьте:

✅ **Google Sheets** - данные должны сохраняться
✅ **Email** - должно прийти письмо на `ktv@kaykovmedia.com`
✅ **Логи Apps Script** - должны быть записи об успешной отправке
✅ **Консоль браузера** - должны быть логи отправки

## Если всё ещё не работает:

1. Сделайте скриншот логов из Apps Script (Executions)
2. Сделайте скриншот консоли браузера
3. Проверьте, сохраняются ли данные в Google Sheets
4. Проверьте, есть ли записи в localStorage (F12 → Application → Local Storage)



