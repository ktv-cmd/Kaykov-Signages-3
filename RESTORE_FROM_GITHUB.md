# 🔄 Как восстановить проект из GitHub

## ✅ Текущий статус

**Все файлы проекта сохранены в GitHub!**

- 📦 **183 файла** отслеживаются git
- ✅ Все изменения закоммичены
- ✅ Все изменения отправлены на GitHub
- 🔗 Репозиторий: `git@github.com:ktv-cmd/Kaykov-Signages-3.git`

---

## 🚀 Восстановление проекта (если удалили файлы)

### Вариант 1: Клонировать заново

```bash
# Перейдите в папку, где хотите восстановить проект
cd ~/Documents

# Клонируйте репозиторий
git clone git@github.com:ktv-cmd/Kaykov-Signages-3.git

# Перейдите в папку проекта
cd Kaykov-Signages-3

# Установите зависимости
npm install

# Запустите проект
npm run dev
```

### Вариант 2: Если папка уже существует

```bash
# Перейдите в папку проекта
cd "path/to/signage website"

# Если папка .git существует, просто обновите:
git pull origin main

# Если папки .git нет, подключите репозиторий:
git init
git remote add origin git@github.com:ktv-cmd/Kaykov-Signages-3.git
git fetch origin
git checkout -b main origin/main
npm install
```

---

## 📋 Что сохранено в GitHub

✅ **Весь исходный код:**
- Все компоненты React (`.tsx` файлы)
- Все страницы
- Все стили и конфигурации

✅ **Конфигурационные файлы:**
- `package.json` - зависимости проекта
- `tsconfig.json` - настройки TypeScript
- `vite.config.ts` - настройки Vite
- `tailwind.config.ts` - настройки Tailwind
- Все остальные конфиги

✅ **Ассеты:**
- Все изображения из `src/assets/`
- Все публичные файлы из `public/`

✅ **Документация:**
- `GOOGLE_APPS_SCRIPT_FINAL.js`
- `push-to-github.sh`
- Все `.md` файлы

---

## ⚠️ Что НЕ сохраняется (и это нормально)

❌ `node_modules/` - устанавливается через `npm install`
❌ `dist/` - генерируется при сборке
❌ `.DS_Store` - системные файлы macOS
❌ `.env` - переменные окружения (если есть)

---

## 🔐 Проверка подключения к GitHub

```bash
# Проверить подключение
ssh -T git@github.com

# Проверить статус
git status

# Проверить удаленный репозиторий
git remote -v

# Посмотреть все коммиты
git log --oneline
```

---

## 💡 Полезные команды

```bash
# Посмотреть все файлы в репозитории
git ls-files

# Посмотреть историю изменений
git log --oneline --graph

# Создать резервную копию текущего состояния
git tag backup-$(date +%Y%m%d)
git push origin backup-$(date +%Y%m%d)
```

---

## 🆘 Если что-то пошло не так

1. **Проверьте SSH ключ:**
   ```bash
   cat ~/.ssh/id_ed25519_github.pub
   ```
   Добавьте его в GitHub: https://github.com/settings/keys

2. **Используйте HTTPS вместо SSH:**
   ```bash
   git remote set-url origin https://github.com/ktv-cmd/Kaykov-Signages-3.git
   ```

3. **Проверьте репозиторий на GitHub:**
   https://github.com/ktv-cmd/Kaykov-Signages-3

---

✅ **Ваш проект полностью сохранен в GitHub!**  
Даже если удалите все файлы на ноутбуке, вы всегда сможете восстановить проект командой `git clone`.

