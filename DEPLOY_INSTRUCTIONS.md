# Инструкции по публикации сайта

Этот сайт можно опубликовать несколькими способами. Выберите наиболее подходящий для вас.

## 🚀 Способ 1: Через Lovable (самый простой)

Если ваш проект создан в Lovable:

1. Откройте https://lovable.dev/projects/4b38477c-724b-45c2-aba6-e03395d9ddd8
2. Нажмите на кнопку **"Share"** в правом верхнем углу
3. Выберите **"Publish"**
4. Следуйте инструкциям на экране
5. Ваш сайт будет доступен по ссылке типа: `https://your-project-name.lovable.app`

## 🌐 Способ 2: Vercel (рекомендуется - бесплатно)

### Шаг 1: Подготовка
```bash
# Убедитесь, что у вас установлен Node.js
# Затем выполните сборку проекта:
npm run build
```

### Шаг 2: Регистрация на Vercel
1. Перейдите на https://vercel.com
2. Зарегистрируйтесь через GitHub (бесплатно)
3. Нажмите **"Add New Project"**

### Шаг 3: Деплой
1. Подключите ваш GitHub репозиторий (или загрузите папку `dist`)
2. Настройки проекта:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
3. Нажмите **"Deploy"**
4. Через 1-2 минуты ваш сайт будет доступен по ссылке типа: `https://your-project.vercel.app`

### Шаг 4: Подключение домена (опционально)
1. В настройках проекта выберите **"Domains"**
2. Добавьте ваш домен (например, `kaykovmedia.com`)
3. Следуйте инструкциям по настройке DNS

## 🔷 Способ 3: Netlify (бесплатно)

### Шаг 1: Подготовка
```bash
npm run build
```

### Шаг 2: Деплой через Netlify
1. Перейдите на https://netlify.com
2. Зарегистрируйтесь (бесплатно)
3. Нажмите **"Add new site"** → **"Deploy manually"**
4. Перетащите папку `dist` в окно браузера
5. Или используйте **"Import from Git"** для автоматического деплоя

### Настройки (если используете Git):
- **Build command**: `npm run build`
- **Publish directory**: `dist`

## 📦 Способ 4: GitHub Pages (бесплатно)

### Шаг 1: Установите gh-pages
```bash
npm install --save-dev gh-pages
```

### Шаг 2: Добавьте скрипты в package.json
```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "homepage": "https://your-username.github.io/your-repo-name"
}
```

### Шаг 3: Деплой
```bash
npm run deploy
```

## 🌍 Способ 5: Любой хостинг (через FTP)

1. Выполните сборку:
```bash
npm run build
```

2. Загрузите содержимое папки `dist` на ваш хостинг через FTP:
   - Все файлы из папки `dist` должны быть в корне сайта
   - Убедитесь, что `index.html` находится в корне

## ⚙️ Важные настройки перед деплоем

### 1. Проверьте переменные окружения
Убедитесь, что все API ключи и настройки правильно настроены для продакшена.

### 2. Настройте базовый путь (если нужно)
Если сайт будет размещен в подпапке, добавьте в `vite.config.ts`:
```typescript
export default defineConfig({
  base: '/your-subfolder/', // если нужен подпуть
  // ... остальные настройки
})
```

### 3. Проверьте роутинг
Убедитесь, что все маршруты работают правильно. Для SPA (Single Page Application) может потребоваться настройка редиректов.

## 📝 Рекомендации

1. **Для быстрого старта**: Используйте Vercel - это самый простой и быстрый способ
2. **Для бесплатного хостинга**: Vercel, Netlify или GitHub Pages
3. **Для собственного домена**: Любой из перечисленных способов поддерживает кастомные домены

## 🔍 Проверка после деплоя

После публикации проверьте:
- ✅ Все страницы открываются
- ✅ Изображения загружаются
- ✅ Формы работают
- ✅ Все ссылки функционируют
- ✅ Сайт работает на мобильных устройствах

## ❓ Проблемы?

Если возникли проблемы:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что все зависимости установлены
3. Проверьте логи деплоя на платформе хостинга

