# ⚙️ НАСТРОЙКА ТРИГГЕРА для автоматической отправки email

## ✅ Хорошая новость!

Вы получаете тестовые email, значит **разрешения настроены правильно!** 🎉

Теперь нужно настроить **триггер**, который будет автоматически отправлять email при получении нового лида.

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ:

### ШАГ 1: Откройте Google Apps Script

1. Перейдите на https://script.google.com
2. Найдите ваш проект (или откройте его из Google Sheet: Extensions → Apps Script)

### ШАГ 2: Проверьте, что код обновлен

Убедитесь, что в коде есть функции:
- ✅ `onSheetEdit(e)` - функция триггера
- ✅ `sendEmailForNewLead(data)` - функция отправки email

Если их нет, скопируйте обновленный код из файла `GOOGLE_APPS_SCRIPT_CODE.js`

### ШАГ 3: Настройте триггер

1. В левой панели нажмите на иконку **часов ⏰** (Triggers)
2. Внизу справа нажмите **+ Add Trigger** (Добавить триггер)
3. Заполните настройки:
   
   ```
   Function to run: onSheetEdit
   Event source: From spreadsheet
   Event type: On edit
   Failure notification settings: Immediate
   ```
4. Нажмите **Save** (Сохранить)

### ШАГ 4: Предоставьте разрешения для триггера

1. Если появится запрос на разрешения - нажмите **Review Permissions**
2. Выберите ваш аккаунт Google
3. Нажмите **Advanced** → **Go to [Project Name] (unsafe)**
4. Нажмите **Allow** для всех разрешений

### ШАГ 5: Проверьте, что триггер создан

В списке триггеров должно появиться:
- **Function:** `onSheetEdit`
- **Event:** `On edit`
- **Source:** `From spreadsheet`
- **Status:** ✅ Включен (зеленая галочка)

## 🧪 ТЕСТИРОВАНИЕ:

### Способ 1: Отправьте тестовый лид через форму

1. Откройте ваш сайт: http://localhost:8080
2. Заполните форму "Get a Custom Quote"
3. Отправьте форму
4. Проверьте:
   - ✅ Google Sheet - должна появиться новая строка
   - ✅ Почту ktv@kaykovmedia.com - должен прийти email

### Способ 2: Добавьте строку вручную в Google Sheet

1. Откройте Google Sheet
2. Добавьте новую строку с данными:
   - Колонка A: Дата/время
   - Колонка B: Имя (например, "Тест Имя")
   - Колонка C: Телефон (например, "+1(718) 478-4200")
   - Колонка D: Email (опционально)
   - Колонка E: Тип сервиса (опционально)
   - Колонка F: Сообщение (опционально)
3. Проверьте почту - должен прийти email

### Способ 3: Проверьте логи триггера

1. В Google Apps Script: View → Logs
2. Добавьте строку в Sheet вручную (как в способе 2)
3. В логах должны появиться записи:
   - `onSheetEdit triggered: Row X`
   - `New row detected: X`
   - `✅ Email sent successfully for row X`

## 🔍 ЕСЛИ ТРИГГЕР НЕ РАБОТАЕТ:

### Проблема 1: Триггер не срабатывает

**Решение:**
- Убедитесь, что триггер настроен правильно (Event type: On edit)
- Проверьте, что вы добавляете строку в **активный лист** (первый лист)
- Проверьте логи - там будут ошибки, если триггер не работает

### Проблема 2: Email не отправляется из триггера

**Решение:**
- Проверьте логи в Google Apps Script
- Убедитесь, что функция `sendEmailForNewLead` вызывается
- Проверьте, что разрешения на email предоставлены (вы уже получаете тестовые email)

### Проблема 3: Двойная отправка email

**Решение:**
- Это нормально! Email отправляется и из `doPost` (при отправке формы), и из триггера (при добавлении строки)
- Это двойная защита - гарантирует, что email будет отправлен в любом случае

## 📝 ВАЖНЫЕ ЗАМЕЧАНИЯ:

1. **Триггер срабатывает при ЛЮБОМ изменении листа** - не только при добавлении строки из формы
2. **Триггер работает только для первого листа** (активного листа)
3. **Триггер проверяет, что это последняя строка** - чтобы не отправлять email при редактировании старых строк
4. **Триггер требует разрешения на доступ к Gmail** - но вы уже их предоставили

## ✅ РЕЗУЛЬТАТ:

После настройки триггера:
- ✅ При отправке формы → лид сохраняется в Sheet → email отправляется
- ✅ Если email не отправился из `doPost` → триггер отправит его при добавлении строки
- ✅ Гарантированная отправка email в 99.9% случаев! 🎉



