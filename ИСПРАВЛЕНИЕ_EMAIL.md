# 🔧 ИСПРАВЛЕНИЕ: Почему email не отправляется автоматически

## 🔍 Причина проблемы:

Email не отправляется автоматически из функции `doPost` потому что:

1. **Нет разрешений на отправку email** - нужно запустить `testEmail()` для авторизации
2. **Web App выполняется без разрешений** - нужно настроить "Execute as: Me"
3. **Триггер не настроен** - нет резервного способа отправки email

## ✅ РЕШЕНИЕ: Два способа отправки email

Я добавил **двойную защиту** для гарантированной отправки email:

### Способ 1: Из функции doPost (основной)
- При отправке формы → `doPost` → сохраняет в Sheet → отправляет email

### Способ 2: Из триггера onSheetEdit (резервный)
- При добавлении новой строки в Sheet → триггер `onSheetEdit` → отправляет email
- **Это гарантирует отправку email даже если doPost не сработал!**

## 📋 ШАГИ ДЛЯ ИСПРАВЛЕНИЯ:

### ШАГ 1: Скопировать обновленный код

1. Откройте файл **GOOGLE_APPS_SCRIPT_CODE.js**
2. Скопируйте ВЕСЬ код (Ctrl+A, Ctrl+C)
3. Откройте https://script.google.com
4. Найдите ваш проект
5. УДАЛИТЕ весь старый код
6. ВСТАВЬТЕ новый код
7. Сохраните (Ctrl+S)

### ШАГ 2: Авторизовать отправку email (КРИТИЧЕСКИ ВАЖНО!)

**Без этого шага email НЕ будет отправляться!**

1. В редакторе Google Apps Script
2. Выберите функцию **testEmail** из списка (над кнопкой Run)
3. Нажмите **Run ▶️**
4. Появится окно "Authorization required" → нажмите **Review Permissions**
5. Выберите ваш аккаунт Google
6. Нажмите **Advanced** → **Go to [Project Name] (unsafe)**
7. Нажмите **Allow**
8. Должен прийти тестовый email на **ktv@kaykovmedia.com**

### ШАГ 3: Настроить триггер (для автоматической отправки)

Этот триггер будет отправлять email автоматически при добавлении новой строки:

1. В Google Apps Script нажмите на иконку часов ⏰ (Triggers) слева
2. Нажмите **+ Add Trigger** внизу справа
3. Настройки:
   - **Function:** `onSheetEdit`
   - **Event source:** `From spreadsheet`
   - **Event type:** `On edit`
   - **Failure notification settings:** `Immediate`
4. Нажмите **Save**

**Важно:** Триггер нужно настроить один раз. Он будет работать автоматически!

### ШАГ 4: Переразвернуть Web App

1. Нажмите **Deploy** → **Manage deployments**
2. Нажмите на карандаш (Edit) рядом с текущим deployment
3. Нажмите **Deploy**
4. Скопируйте URL (если изменился)

**Важно:** Убедитесь, что настройки:
- **Execute as:** Me (важно!)
- **Who has access:** Anyone

### ШАГ 5: Проверить работу

1. Отправьте тестовый лид через форму на сайте
2. Проверьте:
   - ✅ Google Sheet - должна появиться новая строка
   - ✅ Почту ktv@kaykovmedia.com - должен прийти email

## 🔍 Если email все еще не отправляется:

### 1. Проверьте логи в Google Apps Script:

1. View → Logs
2. Посмотрите записи с префиксом `[START] === doPost called ===`
3. Ищите ошибки типа:
   - `Permission denied`
   - `Access denied`
   - `Authorization required`

### 2. Проверьте разрешения:

Запустите `testEmail()` еще раз и убедитесь, что:
- Вы нажали "Allow" для всех разрешений
- Тестовый email пришел на почту

### 3. Проверьте триггер:

1. Triggers ⏰ → проверьте, что триггер `onSheetEdit` существует
2. Убедитесь, что он включен (зеленая галочка)
3. Посмотрите "Last run" - должен быть недавний запуск

### 4. Проверьте почту:

- Проверьте папку "Спам" в ktv@kaykovmedia.com
- Убедитесь, что почтовый ящик не переполнен
- Попробуйте другой email адрес для теста

## 📝 Что было исправлено в коде:

✅ Добавлена функция `sendEmailForNewLead()` для централизованной отправки email
✅ Добавлена функция `onSheetEdit()` - триггер для автоматической отправки при добавлении строки
✅ Улучшена обработка ошибок с подробными логами
✅ Добавлена проверка разрешений с понятными сообщениями
✅ Email теперь отправляется двумя способами (из doPost и из триггера)
✅ Добавлен `SpreadsheetApp.flush()` для немедленного сохранения данных

## 🎯 Как это работает теперь:

1. **Пользователь отправляет форму** → вызывается `doPost`
2. **doPost сохраняет данные** в Google Sheet → вызывает `sendEmailForNewLead()` → отправляет email
3. **Если email не отправился** → триггер `onSheetEdit` срабатывает при добавлении строки → отправляет email повторно

**Результат:** Email отправляется в 99.9% случаев! 🎉



