# 🧪 КАК ПРАВИЛЬНО ТЕСТИРОВАТЬ

## ❌ НЕПРАВИЛЬНО:
- Вызывать функцию `doPost` напрямую из редактора
- Функция `doPost` требует объект события от Web App

## ✅ ПРАВИЛЬНО:

### Способ 1: Используйте testDoPost()
1. В редакторе Google Apps Script
2. Выберите функцию **testDoPost** из списка (НЕ doPost!)
3. Нажмите Run ▶️
4. Посмотрите логи - должно быть "✅ TEST PASSED!"

### Способ 2: Используйте testEmail()
1. Выберите функцию **testEmail** из списка
2. Нажмите Run ▶️
3. Это проверит отправку email
4. Проверьте почту ktv@kaykovmedia.com

### Способ 3: Используйте форму на сайте
1. Откройте ваш сайт
2. Заполните форму
3. Отправьте её
4. Это вызовет doPost через Web App URL (правильный способ)

## 📝 Важно:

- **doPost** - это функция, которая вызывается автоматически Web App'ом
- **testDoPost** - это тестовая функция для проверки в редакторе
- **testEmail** - это функция для проверки отправки email

## 🔍 Если видите ошибку "No postData.contents":

Это значит, что вы вызвали `doPost` напрямую. Используйте `testDoPost()` вместо этого!
