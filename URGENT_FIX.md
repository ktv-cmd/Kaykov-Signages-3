# ⚠️ СРОЧНО: Исправление проблемы

## ❌ Проблема обнаружена:
Тесты показывают: **"Script function not found: doGet"**

Это значит, что код в Google Apps Script **НЕ ОБНОВЛЁН** или Web App **НЕ РАЗВЁРНУТ**.

## ✅ Решение (выполните ПО ПОРЯДКУ):

### ШАГ 1: Обновите код в Google Apps Script

1. Откройте: https://script.google.com
2. Найдите ваш проект (или создайте новый)
3. **УДАЛИТЕ ВСЁ** в редакторе
4. Откройте файл `GOOGLE_APPS_SCRIPT_CODE.js` из этого проекта
5. **СКОПИРУЙТЕ ВЕСЬ КОД** из файла
6. **ВСТАВЬТЕ** в редактор Google Apps Script
7. Нажмите **Save** (Ctrl+S или Cmd+S)

### ШАГ 2: Запустите testEmail() для авторизации

1. В редакторе выберите функцию `testEmail` из выпадающего списка
2. Нажмите ▶️ **Run**
3. Google покажет диалог "Authorization required"
4. Нажмите **Review Permissions**
5. Выберите ваш аккаунт
6. Нажмите **Advanced** → **Go to [Project Name] (unsafe)**
7. Нажмите **Allow**
8. **ВАЖНО:** Дождитесь завершения выполнения
9. Проверьте почту `ktv@kaykovmedia.com` - должно прийти тестовое письмо

### ШАГ 3: Переразверните Web App (КРИТИЧНО!)

1. Нажмите **Deploy** → **New deployment** (или **Manage deployments**)
2. Если есть существующий deployment:
   - Нажмите ✏️ **Edit** (карандаш)
   - Нажмите **Deploy**
3. Если создаёте новый:
   - Нажмите **Deploy**
   - Выберите тип: **Web app**
4. Убедитесь, что настройки:
   - **Execute as:** Me
   - **Who has access:** Anyone (или Anyone with Google account)
5. Нажмите **Deploy**
6. **СКОПИРУЙТЕ НОВЫЙ URL** (если он изменился)

### ШАГ 4: Обновите URL в коде сайта (если URL изменился)

1. Откройте файл `src/lib/googleSheets.ts`
2. Найдите строку с `SCRIPT_URL`
3. Замените URL на новый из шага 3
4. Сохраните файл

### ШАГ 5: Проверьте снова

Запустите тест:
```bash
./check_setup.sh
```

Должен вернуться успешный ответ с JSON.

## 🔍 Проверка результатов:

### После выполнения всех шагов:

1. **Тест подключения должен показать:**
   ```
   ✅ SUCCESS: Script is accessible
   ```

2. **Тест отправки формы должен показать:**
   ```
   ✅ SUCCESS: Form submission worked
   ✅ Email was sent successfully!
   ```

3. **Проверьте:**
   - ✅ Почту `ktv@kaykovmedia.com` (и папку Спам)
   - ✅ Google Sheets - должны быть данные
   - ✅ Логи в Executions - должно быть `✅ Email sent successfully`

## 🆘 Если всё ещё не работает:

1. **Проверьте логи в Google Apps Script:**
   - Откройте Executions (⏰)
   - Посмотрите последние выполнения
   - Найдите ошибки

2. **Проверьте разрешения:**
   - Убедитесь, что предоставили ВСЕ разрешения
   - Попробуйте запустить `testEmail()` ещё раз

3. **Проверьте Web App настройки:**
   - Execute as: **Me** (обязательно!)
   - Who has access: **Anyone**

4. **Используйте тестовую страницу:**
   - Откройте `DETAILED_TEST.html` в браузере
   - Выполните тесты там

## 📝 Файлы для справки:

- `GOOGLE_APPS_SCRIPT_CODE.js` - код для вставки в Apps Script
- `COMPREHENSIVE_TEST.js` - тесты для запуска в Apps Script
- `check_setup.sh` - скрипт для проверки подключения
- `DETAILED_TEST.html` - тестовая страница
- `AUTO_SETUP_CHECK.md` - подробная инструкция

## ⚡ Быстрая проверка:

После выполнения всех шагов, запустите:
```bash
./check_setup.sh
```

Если тест показывает ошибки - вернитесь к соответствующему шагу.



